{
  "name": "store-db",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "yarn && yarn exec sequelize db:migrate",
    "reset-migrate": "yarn && yarn exec sequelize db:migrate:undo --to store-db && yarn exec sequelize db:migrate",
    "undo-migrate": "yarn && yarn exec sequelize db:migrate:undo:all --to store-db",
    "dev": "nodemon server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.2",
    "express": "^4.18.1",
    "pg": "^8.8.0",
    "pg-hstore": "^2.3.4",
    "sequelize": "^6.20.1",
    "sequelize-cli": "^6.4.1"
  }
}
